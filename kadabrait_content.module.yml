<?php

/**
 * Implement hook_theme().
 */
function kadabrait_content_theme($existing, $type, $theme, $path) {
    return [
      'kadabrait_content' => [
        'variables' => ['items' => [], 'title' => '' ]
      ]
    ];
}